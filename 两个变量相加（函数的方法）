#inlcude<stdio.h>

int Sum(int a, int b)
{
	int value = a + b;
	return value;
}

int main()
{
	int a = 0;
	int b = 0;
	int sum = 0;
	printf("input a and b:");
	scanf("%d %d", &a, &b);
	sum = Sum(a, b);
	printf("sum = %d\n",sum);
	return 0;
}
